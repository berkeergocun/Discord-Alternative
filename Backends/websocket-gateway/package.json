{
  "name": "websocket-gateway",
  "version": "1.0.0",
  "description": "Real-time WebSocket gateway for events broadcasting",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun"
  },
  "dependencies": {
    "elysia": "^1.0.0",
    "@elysiajs/cors": "^1.0.0",
    "@elysiajs/swagger": "^1.0.0",
    "ioredis": "^5.3.0",
    "amqplib": "^0.10.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/amqplib": "^0.10.0",
    "@types/ws": "^8.5.0"
  }
}
